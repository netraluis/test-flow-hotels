services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    # container_name: nextjs-app # Comentado para evitar conflictos de nombre en despliegues múltiples
    restart: always
    env_file:
      - .env
    labels:
      - "traefik.http.middlewares.auth.basicauth.users=theagentslab:$apr1$MwlcGPR4$a5bXs72Cxw6JrKBRTutyP1"
      - "traefik.http.routers.mcphotels-testflowhotels-8kzfd1.middlewares=auth"